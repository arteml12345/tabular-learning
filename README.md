# hybrid_embed

Hybrid tabular learning framework: PyTorch embeddings fed to classical predictors.

## Setup

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Run tests

```bash
pytest tests/
```
