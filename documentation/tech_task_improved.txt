# Technical Specification (Improved)

## Hybrid Tabular Framework: PyTorch Embeddings -> Classical Predictor (Embeddings-Only)

### Project Context

Build a tabular learning framework for a PhD thesis. The idea is to eliminate
"expert" feature engineering in tabular data machine learning. One passes
tabular data "as is" to the framework and gets a trained model for prediction.
The framework uses deep learning to produce embeddings (learned feature
representations) and then feeds them to classical tabular ML algorithms.
This combines deep learning flexibility with the strong performance of
classical models like XGBoost on tabular data -- all without expert feature
engineering.

---

## 0. Goal

Implement a Python framework that, given a tabular dataset and configuration,
automatically:

1. Infers schema (numeric / categorical)
2. Validates input data (defensive checks)
3. Trains / tunes a PyTorch tabular embedding model
4. Extracts embeddings for all samples
5. Tunes / trains a classical predictor on embeddings only
6. Evaluates and logs results reproducibly across benchmark datasets

**Hard constraints**

* Deep learning: **PyTorch only** (no TensorFlow / Keras)
* **No manual feature engineering** (no handcrafted features; no expert steps)
* Classical models must use **embeddings only** as input
* Tasks: **binary classification, multiclass classification, regression**
* Hyperparameter tuning: **Hyperopt** (not Optuna, not Ray Tune)
* Experiment tracking: **no MLflow** (local JSON / CSV / Parquet logging only)

---

## 1. Key Design Principle: "Embeddings-only"

The framework must treat the tabular dataset as "raw" inputs and perform only:

* Minimal **schema-driven normalization** needed for stable training (allowed)
* Categorical **index mapping** for embedding tables (allowed)
* Missing-value handling (allowed, but must be non-expert, generic)
* Input **data validation** -- defensive checks (allowed, generic)

**Forbidden**

* One-hot encoding, target encoding, count encoding
* Domain feature construction
* Binning, polynomial features, interactions
* Feature selection
* Any custom dataset-specific engineering

---

## 2. Pipeline Overview

**Input**:

* `X` : `pd.DataFrame` -- raw tabular features (no preprocessing
  needed; the framework handles schema inference, missing values,
  scaling, and categorical encoding internally).
* `y` : `pd.Series | np.ndarray` -- target variable (binary labels,
  multiclass labels, or continuous values depending on task).
* `task_config` : `TaskConfig` -- specifies the task type: `"binary"`,
  `"multiclass"`, or `"regression"`.
* `embedding_step` : `EmbeddingStepConfig` -- the user's choice of ONE
  embedding model and its HP search space. E.g.,
  `EmbeddingStepConfig(model_type="mlp")` or
  `EmbeddingStepConfig(model_type="tab_transformer", search_space={...})`.
* `classical_step` : `ClassicalStepConfig` -- the user's choice of ONE
  classical model and its HP search space. E.g.,
  `ClassicalStepConfig(model_type="xgboost")` or
  `ClassicalStepConfig(model_type="custom", model_class=SVC, search_space={...})`.
* `budget_config` : `BudgetConfig` -- HPO budget limits (max trials,
  max epochs, time budgets).
* `run_config` : `RunConfig` (optional) -- experiment settings: number
  of CV folds, seed, device, output directory, scaler choice, etc.

**Output**: `RunResult` + local artifacts folder

### 2.1 Stages

1. **Data validation** (defensive checks)
2. **Data profiling + schema inference**
3. **Split generation** (CV or train/val/test)
4. **HPO split** (one held-out split for both embedding and classical HPO)
5. **Minimal preprocessing** (fitted on HPO-train only)
6. **Embedding model HPO** (Hyperopt on HPO-split)
7. **Classical model HPO** (Hyperopt on HPO-split embeddings)
8. **Per-fold evaluation** (apply best configs across all K folds)
9. **Aggregation + reporting**
10. **Local logging + artifacts**

---

## 3. Supported Tasks (Only)

* Binary classification
* Multiclass classification
* Regression

No ranking. No multi-output regression.

---

## 4. Data Validation (Generic, Defensive)

Before any processing, the framework must check:

* **Constant columns** (zero variance) -- drop with warning, since they break
  StandardScaler and carry no information
* **All-NaN columns** -- drop with warning
* **Target variable validity** -- no NaN in y; correct number of classes for
  the declared task; at least 2 classes for classification
* **Empty dataset** -- raise error if X has 0 rows or 0 columns after
  validation

These are generic defensive checks, not expert feature engineering.

---

## 5. Target Variable Handling

### 5.1 Classification

* Binary: infer positive class as the higher-valued label (or alphabetically
  second for strings). Allow override via config.
* Multiclass: apply sklearn `LabelEncoder` to map string / non-contiguous
  integer labels to 0..K-1. Store the encoder for inverse transform.

### 5.2 Regression

* Apply StandardScaler to y (fitted on train split only). This is critical
  for stable DL training with MSE loss.
* Store scaler; inverse-transform predictions before evaluation so metrics
  are in original scale.

---

## 6. Minimal Preprocessing Rules (Generic, Non-expert)

### 6.1 Missing values

* Numeric: fill with train-fold median. 
* Missingness indicator column allowed and added automatically (if ste to true in run configurations)
* Categorical: map missing to a dedicated `"__MISSING__"` category index.

### 6.2 Numeric scaling (allowed)

* Default: StandardScaler fitted on train fold only
* Config toggle: RobustScaler (for datasets with extreme outliers)

### 6.3 Categorical mapping (allowed)

* Per categorical column: fit vocabulary on train fold
* Unseen values in val/test -> `"__OOV__"` index
* High-cardinality handling: if a categorical column has cardinality > a
  configurable threshold (default: 1000), cap the vocabulary to the top-N
  most frequent values and map the rest to `"__OOV__"`. This is a generic
  strategy, not domain engineering.

**Important**: Classical models never see categoricals directly. They only
see learned embedding output vectors.

---

## 7. Embedding Stage (Deep Learning, PyTorch)

### 7.1 Requirements

Embedding stage must output a **fixed-length dense vector** per sample:

* `E in R^(N x d)`
* Used as the *only* input to classical models.

### 7.2 Device Management

* Auto-detect available device: CUDA > MPS > CPU
* Allow override via config: `device: "cuda" | "mps" | "cpu" | "auto"`
* All embedding training happens on the selected device
* Encoding output is always returned as CPU numpy array (for classical models)

### 7.3 Embedding Model Selection (user's choice)

The user selects ONE embedding model for each experiment run. The
framework tunes its hyperparameters within the given search space.
If the user wants to compare models, they run separate experiments.

#### Mode A -- From-scratch PyTorch architectures

Custom architectures written in raw PyTorch (`nn.Module`).

The framework ships with one built-in from-scratch model:

1. **Tabular MLP with categorical embeddings** (`model_type="mlp"`)

   * Learn embedding tables per categorical feature
   * Numeric projection layer
   * Concatenate -> MLP trunk
   * Output embedding vector from penultimate layer

Usage:

```python
embedding = EmbeddingStepConfig(model_type="mlp")
# or with custom HP space:
embedding = EmbeddingStepConfig(
    model_type="mlp",
    search_space={
        "hidden_dims": hp.choice("h", [[128, 64], [256, 128, 64]]),
        "dropout": hp.uniform("d", 0, 0.3),
        "lr": hp.loguniform("lr", ln(1e-4), ln(1e-2)),
    },
    fixed_params={"activation": "gelu", "use_layer_norm": True},
)
```

#### Mode B -- Library-backed architectures (pytorch-tabular)

For established, published architectures, use the `pytorch-tabular`
library via the adapter. `pytorch-tabular` is a core dependency.

The user selects ONE specific pytorch-tabular model:

* **TabTransformer** (`model_type="tab_transformer"`)
* **TabNet** (`model_type="tabnet"`)
* **GATE** (`model_type="gate"`)
* **CategoryEmbedding** (`model_type="category_embedding"`)
* Others as supported by pytorch-tabular

Usage:

```python
embedding = EmbeddingStepConfig(model_type="tab_transformer")
# or with custom HP space:
embedding = EmbeddingStepConfig(
    model_type="tab_transformer",
    search_space={
        "input_embed_dim": hp.choice("d", [32, 64]),
        "num_attn_blocks": hp.quniform("b", 2, 4, 1),
    },
)
```

#### Common interface

Both Mode A and Mode B models implement `EmbeddingModel`:

* `fit(X_train, y_train, X_val, y_val, config)` -- train as a full
  standalone predictor (with prediction head)
* `predict(X)` / `predict_proba(X)` -- predict using the full model
  (used during HPO to evaluate the model's quality)
* `encode(X) -> np.ndarray` -- extract embeddings from the
  intermediate layer (used after HPO to feed classical models)
* `save(path)` / `load(path)`

### 7.4 Embedding Extraction

The embedding model is trained as a full predictor (input -> hidden
layers -> prediction head). After training, embeddings are extracted
from an intermediate layer -- the model's learned internal
representation.

#### MLP (Mode A)

Architecture: `[input -> hidden_1 -> ... -> hidden_n -> pred_head]`

Extraction point is configurable via `EmbeddingStepConfig.fixed_params`:
* `embedding_layer: int` -- index of the hidden layer to extract from.
  Default: -1 (penultimate layer, i.e., the last hidden layer before
  the prediction head). Can be set to any valid layer index.

The output of the specified hidden layer (after activation) is the
embedding vector.

#### pytorch-tabular models (Mode B)

All pytorch-tabular models share a common architecture:

    [Embedding layer] -> [Backbone] -> [Head]

* **Embedding layer**: encodes categorical (learned embeddings) and
  continuous (linear projection) features
* **Backbone**: model-specific layers (transformer blocks for
  TabTransformer, attention steps for TabNet, gating for GATE, etc.)
* **Head**: final linear layer(s) for prediction

Extraction point: **backbone output** (before the head). This is
consistent across all pytorch-tabular models and is exposed by the
library's architecture. The adapter hooks into the backbone output
to extract embeddings.

| Model             | Backbone output (= embedding)              |
|-------------------|---------------------------------------------|
| TabTransformer    | Concatenated contextual column embeddings   |
|                   | after all transformer blocks                |
| TabNet            | Aggregated feature representation after     |
|                   | all attention steps                         |
| CategoryEmbedding | Last hidden layer before prediction head    |
| GATE              | Gated output before prediction head         |

---

## 8. Hyperparameter Optimization (Hyperopt)

### 8.1 CV + HPO interaction (critical design decision)

**Strategy: HPO once, evaluate across folds.**

1. From the K-fold split, designate fold-0 as the HPO split.
2. Run embedding HPO and classical HPO on fold-0 (train/val).
3. Record the best embedding config and best classical config.
4. For all K folds: train embedding model with best config, extract
   embeddings, train classical model with best config, evaluate on
   test fold.
5. Aggregate metrics across K folds (mean +/- std).

Rationale: full HPO per fold is K times more expensive and impractical
for thesis-scale compute. The single-HPO approach is standard practice
in AutoML literature. The K-fold evaluation still provides robust
performance estimates.

### 8.2 General strategy

Use **Hyperopt TPE** with:

* **Epoch-level early stopping** within each trial (monitor validation
  metric, stop if no improvement for `patience` epochs). This is
  standard early stopping, not cross-trial pruning.
* **Time budget** per stage (wall-clock). If budget is exceeded, stop
  launching new trials and return best result so far.

### 8.3 Objective for embedding HPO

The embedding model is trained as a **standalone predictor** -- it
learns to solve the actual task (classification / regression) end-to-end,
as if there is no classical step. The HPO objective is the model's own
validation score.

Per trial:

1. Train embedding model on HPO-train as a full predictor
   (with prediction head + epoch-level early stopping on HPO-val)
2. Compute the model's validation metric (roc_auc / accuracy / rmse)
3. Return negative score (Hyperopt minimizes)

After HPO finds the best config, the best model is retrained and
embeddings are extracted from an intermediate layer (see Section 7.4).
The classical model then consumes these embeddings.

Rationale: training the NN to solve the actual task produces
representations that capture the structure of the problem. These
learned internal representations are the embeddings. No proxy model
is needed -- the model's own prediction quality directly indicates
how good its learned representations are.

### 8.4 Search spaces

Each model type has a **default search space** defined in the
framework. The user can override it partially or fully via
`EmbeddingStepConfig.search_space` or
`ClassicalStepConfig.search_space`.

If `search_space=None` (default), the framework uses its built-in
default space. If the user provides a search space, ONLY those
parameters are tuned -- parameters not in the space use values from
`fixed_params` or the model's defaults.

#### Default MLP search space (Mode A)

* embedding_dim: 32--256
* MLP depth: 2--8 layers
* hidden width: 128--2048
* dropout: 0--0.5
* activation: ReLU / GELU / SiLU
* normalization: LayerNorm on/off
* lr: log-uniform (1e-5 to 1e-2)
* weight_decay: log-uniform (1e-6 to 1e-2)
* batch_size: categorical choices (256, 512, 1024, 2048)
* optimizer: AdamW (fixed)

#### Default TabTransformer search space (Mode B)

* input_embed_dim: 32--128
* num_heads: 2--8
* num_attn_blocks: 2--6
* attn_dropout: 0--0.3
* add_shared_embedding: True / False
* lr: log-uniform (1e-5 to 1e-3)
* weight_decay: log-uniform (1e-6 to 1e-2)
* batch_size: categorical choices (128, 256, 512)

Other pytorch-tabular models have their own default spaces.

#### Default classical model search spaces

Each built-in classical model has a default space. See
`classical/model_zoo.py` for full definitions. User overrides
replace the entire space for the selected model.

---

## 9. Classical Model Stage (Embeddings-only)

### 9.1 Embedding model retraining for final evaluation

For each CV fold's final evaluation:

1. Train embedding model on fold's train set as a **standalone
   predictor** using best HPO config (with early stopping on
   fold's val set).
2. Extract embeddings from the model's intermediate layer
   (see Section 7.4) for train, val, and test sets.
3. Train classical model on train embeddings.
4. Evaluate on test embeddings.

The embedding model is NOT retrained on train+val combined. The val set
is used only for early stopping. This keeps the evaluation clean and
avoids data leakage concerns.

### 9.2 Classical model selection (user's choice)

The user selects ONE classical model for each experiment run. The
framework does not search across model families -- the user makes
that research decision.

Any sklearn-compatible model can be used. The framework ships with
default HP spaces for common models, but the user can provide any
model class + HP space.

**Built-in models with default HP spaces:**

Classification: LogisticRegression, RandomForestClassifier,
ExtraTreesClassifier, XGBoostClassifier, LightGBMClassifier,
CatBoostClassifier.

Regression: Ridge, ElasticNet, RandomForestRegressor,
ExtraTreesRegressor, XGBoostRegressor, LightGBMRegressor,
CatBoostRegressor.

**Using a built-in model (defaults):**

```python
classical = ClassicalStepConfig(model_type="xgboost")
```

**Using a built-in model with custom HP space:**

```python
classical = ClassicalStepConfig(
    model_type="xgboost",
    search_space={
        "max_depth": hp.quniform("max_depth", 3, 8, 1),
        "learning_rate": hp.loguniform("lr", ln(0.01), ln(0.3)),
    },
)
```

**Using any custom sklearn-compatible class:**

```python
from sklearn.svm import SVC

classical = ClassicalStepConfig(
    model_type="custom",
    model_class=SVC,
    search_space={
        "C": hp.loguniform("C", ln(1e-3), ln(1e3)),
        "kernel": hp.choice("kernel", ["rbf", "linear"]),
    },
    fixed_params={"probability": True},
)
```

Requirements for any model:
* Must have `fit(X, y)` and `predict(X)` methods.
* For classification: must also have `predict_proba(X)`.
* All search_space keys must be valid constructor arguments.
* `fixed_params` are always passed to the constructor (not tuned).
* `supports_early_stopping`: if True, framework passes eval_set
  and early_stopping_rounds to fit() (for XGBoost/LightGBM/CatBoost).

### 9.3 Classical model tuning (Hyperopt)

Hyperopt tunes the hyperparameters of the ONE selected model.
There is no cross-model selection -- the user picks the model,
Hyperopt finds the best hyperparameters within its search space.

Early stopping for boosted models:

* LightGBM / XGBoost: use built-in early stopping with validation set
* CatBoost: use built-in early stopping

**Output**

* `best_classical_params` (best HP config found)
* Per fold: train with best params on fold-train embeddings,
  evaluate on fold-test

---

## 10. Evaluation Protocol

### 10.1 Splits

Support:

* K-fold CV (default: 5) -- primary mode
* Single train/val/test split (optional, for quick debugging)

For K-fold CV, use stratified splits for classification tasks.

### 10.2 Metrics

**Binary classification**

* Primary: ROC-AUC
* Secondary: log-loss, PR-AUC

**Multiclass**

* Primary: accuracy
* Secondary: macro-F1, log-loss

**Regression**

* Primary: RMSE
* Secondary: MAE, R-squared

### 10.3 Reporting

For CV:

* Per-fold metrics
* Mean +/- std of each metric across folds
* Best embedding config and best classical config used

---

## 11. Logging (No MLflow)

### 11.1 Local artifacts per run

Output folder structure (CV-aware):

```
runs/
  <dataset_id>/
    run_YYYYMMDD_HHMMSS/
      config.yaml              # full run configuration
      schema.json              # inferred schema
      validation_report.json   # data validation results
      hpo/
        embedding_hpo_trials.jsonl
        classical_hpo_trials.jsonl
        best_embedding_config.json
        best_classical_config.json
      folds/
        fold_0/
          embedding_ckpt.pt
          classical_model.pkl
          metrics.json
          predictions.csv      (optional)
          embeddings.npy        (optional toggle)
        fold_1/
          ...
        fold_K/
          ...
      summary_metrics.json     # aggregated mean +/- std
      stdout.log
```

### 11.2 Trial logging format

Each Hyperopt trial appended to JSONL:

* trial_id
* params (full hyperparameter dict)
* train_time_seconds
* val_metric_name
* val_metric_value
* early_stop_epoch (for embedding trials)
* embedding_dim_out (for embedding trials)
* model_type (for classical trials)
* seed

Must be sufficient for thesis tables and analysis.

---

## 12. Reproducibility & Seed Management

### 12.1 Master seed

* A single `master_seed` in config (default: 42)
* All randomness is derived from this seed

### 12.2 Seed derivation

* Per-fold seeds: `fold_seed = master_seed + fold_index`
* Per-trial seeds: `trial_seed = master_seed + trial_id`
* Applied to: Python `random`, `numpy`, `torch` (manual_seed + cuda)

### 12.3 Deterministic mode

* Set `torch.use_deterministic_algorithms(True)` when config flag
  `deterministic=True` (default: False, because it can be slower)
* Always set `torch.manual_seed` and `torch.cuda.manual_seed_all`
* CV splitter uses `random_state=master_seed`

---

## 13. Required Python Stack

### 13.1 Core

* python >= 3.10
* numpy
* pandas
* scikit-learn
* scipy
* pyyaml (for config serialization)

### 13.2 Deep learning

* torch
* pytorch-tabular (Mode B embedding models: TabTransformer, etc.)

### 13.3 Classical models

* lightgbm
* xgboost
* catboost

### 13.4 HPO

* hyperopt

### 13.5 Serialization

* joblib (sklearn / classical models)

### 13.6 Optional helpers

* openml (dataset fetching for benchmarks)

---

## 14. Package Structure

```
hybrid_embed/
  __init__.py
  config.py                        # dataclasses: TaskConfig, BudgetConfig,
                                   #   RunConfig, Schema, RunResult
  utils.py                         # seed management, device helpers, timing

  data/
    __init__.py
    validation.py                  # input data validation (defensive checks)
    schema.py                      # schema inference (numeric/categorical)
    splits.py                      # CV and single-split generation
    preprocess.py                  # scaling, missing values, categorical mapping

  embed/
    __init__.py
    base.py                        # EmbeddingModel ABC
    scratch_mlp.py                 # Mode A: from-scratch MLP
    adapters/
      __init__.py
      pytorch_tabular_adapter.py   # Mode B: pytorch-tabular wrapper

  classical/
    __init__.py
    model_zoo.py                   # model definitions + per-family param spaces

  hpo/
    __init__.py
    embed_search.py                # Hyperopt search for embedding models
    classical_search.py            # Hyperopt search for classical models
    spaces.py                      # search space definitions

  eval/
    __init__.py
    metrics.py                     # metric computation per task type
    runner.py                      # experiment orchestrator (full pipeline)

  log/
    __init__.py
    local_logger.py                # artifact writing, trial JSONL logging

  cli/
    __init__.py
    run_benchmark.py               # CLI entry point
```

---

## 15. Interfaces

### 15.1 EmbeddingModel (ABC)

```python
from abc import ABC, abstractmethod
import numpy as np

class EmbeddingModel(ABC):
    """Base class for all embedding models.

    Each model is trained as a standalone predictor (with a prediction
    head for the target task). After training, embeddings are extracted
    from an intermediate layer (see Section 7.4).
    """

    @abstractmethod
    def fit(self, X_train, y_train, X_val, y_val, config):
        """Train the model as a standalone predictor.

        Parameters
        ----------
        X_train : dict
            Preprocessed train data with keys 'numeric' (np.ndarray)
            and 'categorical' (np.ndarray of int indices).
        y_train : np.ndarray
        X_val : dict
        y_val : np.ndarray
        config : dict
            Hyperparameters for this model.

        Returns
        -------
        self
        """

    @abstractmethod
    def predict(self, X) -> np.ndarray:
        """Predict using the full model (used during HPO)."""

    def predict_proba(self, X) -> np.ndarray:
        """Predict probabilities (classification only)."""
        raise NotImplementedError

    @abstractmethod
    def encode(self, X) -> np.ndarray:
        """Extract embeddings from intermediate layer.

        Parameters
        ----------
        X : dict
            Same format as fit inputs.

        Returns
        -------
        np.ndarray of shape (n_samples, embedding_dim)
        """

    @abstractmethod
    def save(self, path: str) -> None:
        """Save model checkpoint."""

    @classmethod
    @abstractmethod
    def load(cls, path: str) -> "EmbeddingModel":
        """Load model from checkpoint."""
```

### 15.2 Step Configuration (user's choices)

```python
@dataclass
class EmbeddingStepConfig:
    """Configuration for the embedding model step.

    The user selects ONE embedding model and optionally provides
    a custom search space and/or fixed parameters.

    Parameters
    ----------
    model_type : str
        Mode A (from-scratch): "mlp"
        Mode B (pytorch-tabular): "tab_transformer", "tabnet",
            "gate", "category_embedding", etc.
    search_space : dict or None
        Hyperopt search space for this model's hyperparameters.
        If None, uses the built-in default space for model_type.
    fixed_params : dict or None
        Parameters always passed to the model (not tuned).
        These override defaults and are merged with HPO-sampled
        params.
    """
    model_type: str = "mlp"
    search_space: dict | None = None
    fixed_params: dict | None = None

@dataclass
class ClassicalStepConfig:
    """Configuration for the classical model step.

    The user selects ONE classical model and optionally provides
    a custom search space and/or fixed parameters.

    Parameters
    ----------
    model_type : str
        Built-in: "xgboost", "lightgbm", "catboost", "ridge",
            "logistic_regression", "random_forest", "extra_trees",
            "elastic_net".
        Custom: "custom" (must also set model_class).
    model_class : type or None
        Required when model_type="custom". Must be sklearn-compatible
        (fit/predict/predict_proba).
    search_space : dict or None
        Hyperopt search space. If None, uses built-in default
        space for model_type.
    fixed_params : dict or None
        Parameters always passed to the constructor (not tuned).
    supports_early_stopping : bool
        If True, framework passes eval_set and early_stopping_rounds
        to fit(). Default True for xgboost/lightgbm/catboost,
        False for everything else.
    """
    model_type: str = "xgboost"
    model_class: type | None = None
    search_space: dict | None = None
    fixed_params: dict | None = None
    supports_early_stopping: bool | None = None  # None = auto-detect
```

### 15.3 Top-level API (sklearn-style)

```python
class HybridTabularModel:
    """Main entry point. Sklearn-style fit/predict interface.

    Parameters
    ----------
    task : str
        One of 'binary', 'multiclass', 'regression'.
    embedding : EmbeddingStepConfig
        Which embedding model to use and how to tune it.
    classical : ClassicalStepConfig
        Which classical model to use and how to tune it.
    budget_config : BudgetConfig
        HPO budget and time limits.
    run_config : RunConfig, optional
        CV folds, seed, device, logging options.
    """

    def fit(self, X, y):
        """Run full pipeline: validate, infer schema, HPO, train."""

    def predict(self, X):
        """Predict using best embedding + classical model."""

    def predict_proba(self, X):
        """Predict probabilities (classification only)."""
```

This class is the user-facing API. Internally it delegates to the
pipeline stages. It does NOT aim for full sklearn estimator compliance
(no `get_params`/`set_params`/estimator checks) -- just the familiar
`fit`/`predict`/`predict_proba` interface.

### 15.4 Experiment runner (for benchmarking)

```python
def run_experiment(
    X, y,
    task_config,
    embedding_step: EmbeddingStepConfig,
    classical_step: ClassicalStepConfig,
    budget_config,
    run_config,
) -> RunResult:
    """Run full experiment with CV, HPO, evaluation, and logging.

    This is the function used for thesis benchmarking. It orchestrates
    the entire pipeline and produces the artifact folder.

    The user has already chosen one embedding model and one classical
    model. This function tunes their HPs and evaluates via K-fold CV.
    """
```

---

## 16. Recommended Defaults (for thesis benchmarking)

* 5-fold stratified CV (classification) / 5-fold CV (regression)
* Embedding HPO: 30--60 trials, max 50 epochs, patience 8
* Classical HPO: 80--200 trials for the selected model
* Save embeddings toggle: OFF by default (disk heavy)
* Device: "auto"
* Master seed: 42
* Deterministic mode: False

These defaults are tuned for moderate-sized datasets (1k--100k rows).
For very small or very large datasets, adjust max_epochs and trial
counts accordingly via config.

---

## 17. Acceptance Criteria (Definition of Done)

Framework is complete when:

1. **End-to-end runs** on at least:
   * Adult (binary classification)
   * A multiclass dataset (e.g., Covertype or similar from OpenML)
   * California Housing (regression)

2. **Implements**:
   * Mode A: from-scratch MLP embedding + Hyperopt search
   * Mode B: pytorch-tabular adapter (TabTransformer + at least one
     other model) + Hyperopt search
   * Single classical model Hyperopt tuning on embeddings
   * User-overridable HP spaces for both embedding and classical steps
   * Custom user-provided classical model class support
   * Full K-fold CV evaluation pipeline

3. **Produces**:
   * Reproducible metrics and artifact folder per run
   * CV summary table for multiple datasets
   * JSONL trial logs suitable for thesis tables

4. **Ensures**:
   * Classical models consume embeddings only (verified in code)
   * No engineered features created anywhere
   * All results are reproducible given the same seed

5. **Code quality**:
   * All modules documented with numpy-style docstrings
   * All modules covered with unit tests (see Section 19)
   * Data validation catches degenerate inputs gracefully

---

## 18. Non-negotiable Code Quality Requirements

* **Documentation**: numpy-style docstrings on all public classes,
  methods, and functions. This is a scientific project; documentation
  is a first-class concern.
* **API style**: sklearn-style `fit` / `predict` / `encode` interfaces
  on all components. The top-level `HybridTabularModel` class provides
  the familiar sklearn interface for end users.
* **Unit tests**: full coverage following the testing guidelines in
  Section 19.

---

## 19. Unit Testing Guidelines

### Framework

This project uses **pytest** as the testing framework.

### Forbidden Practices

1. **No fixtures** -- NEVER use pytest fixtures. All test data must be
   generated explicitly within each test function or in module-level
   helper functions.
2. **Avoid mocks** whenever possible. Prefer testing with real (small,
   synthetic) data.

### Data Generation

* When a tested function needs data, generate random data **explicitly**
  in the test module using `random`, `numpy`, `pandas`.
* Fix random seeds for reproducibility.
* Make data generation visible and understandable.

### Demo-Purpose Testing

Unit tests serve dual purposes:

* **Testing functionality**: verify correctness with assertions.
* **Documentation**: serve as live examples of how functions work.

Tests should be structured so that anyone can open an ipython console,
run the test code piece by piece, and understand function inputs,
outputs, and behavior.

### Print Statements for Visibility

* Include `print()` statements showing what the test does, intermediate
  values, and results.
* Prints are for debugging and visibility, NOT substitutes for `assert`.
* All tests must use proper `assert` statements for validation.

### Test Structure Example

```python
import numpy as np
from hybrid_embed.data.schema import infer_schema

def generate_mixed_dataframe(n_rows, random_seed=42):
    rng = np.random.RandomState(random_seed)
    return pd.DataFrame({
        "age": rng.randint(18, 80, size=n_rows),
        "income": rng.normal(50000, 15000, size=n_rows),
        "city": rng.choice(["NYC", "LA", "CHI"], size=n_rows),
    })

def test_infer_schema_detects_categorical():
    df = generate_mixed_dataframe(n_rows=100, random_seed=42)
    print(f"DataFrame columns: {list(df.columns)}")
    print(f"Dtypes:\n{df.dtypes}")

    schema = infer_schema(df)
    print(f"Inferred schema: {schema}")

    assert "city" in schema.categorical_columns
    assert "age" in schema.numeric_columns
    assert "income" in schema.numeric_columns
    print("Test passed: schema correctly identified column types")
```

### Key Principles

* Every test is self-contained and runnable independently
* All data generated within the test
* Descriptive prints document test progress
* Proper assert statements validate results
* Tests teach how the function works
